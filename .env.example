# Liga/desliga os dados de demonstração (mock).
# true  = usa dados locais (sem backend) — padrão para testar agora
# false = chama a API real (quando o backend existir)
VITE_USE_MOCK=true
VITE_API_URL=http://localhost:3000
